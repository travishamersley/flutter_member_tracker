name: membership_tracker
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.8

dependencies:
  excel: ^4.0.0
  file_picker: ^8.0.0
  flutter:
    sdk: flutter
  local_auth: ^2.3.0
  intl: ^0.20.2
  path_provider: ^2.1.5
  share_plus: ^10.1.0
  shared_preferences: ^2.5.4
  url_launcher: ^6.3.2
  uuid: ^4.5.2
  signature: ^6.3.0
  camera: ^0.12.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
